# AWS CLI profile (optional; leave blank to use default credentials chain)
AWS_PROFILE=default

# Core environment settings
AWS_REGION=ap-northeast-1
STACK_NAME=calendar-auto-register
PROJECT_NAME=calendar-auto-register

# Container image publishing
# ECR repository URI such as 123456789012.dkr.ecr.ap-northeast-1.amazonaws.com/calendar-auto-register
ECR_IMAGE_REPOSITORY=111111111111.dkr.ecr.ap-northeast-1.amazonaws.com/calendar-auto-register
# Optional override; defaults to current git commit hash when empty
IMAGE_TAG=
